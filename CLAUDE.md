# mysonstudy — 은우 북카드

아이(은우)와 영어 원서를 함께 읽는 부모를 위한 학습 카드 생성 앱. **기본 흐름**은 표지 사진(또는 제목) → AI 판독 → Google Books 보강 → 학습 카드 → 서재. 여기에 세 확장이 얹혀 있다 — ① **유튜브 낭독 자막 grounding**: 책 제목으로 낭독 영상을 자동 검색·선택해 그 자막으로 카드를 실제 본문에 붙이고, 챕터별 **영어 원문+한글 해석 리더**(단어 더블탭 뜻·발음·"모은 단어" 담기)를 만든다. ② **영어단어장 정복**: 교재 사진을 판독(원문 전사) → 영영 정의·이모지 → 영단어 5지선다 시험 → 오답노트. ③ **은우 자유대화**: Sunny 선생님(OpenAI Realtime 실시간 음성 — 관문 R)과 주제·단어장을 골라 5분 전화영어, 화자별 실시간 스크립트 + 말문 막힘 도움 카드·이모지 그림 카드·주제 일러스트, 끝나면 문장을 탭해 선생님 말투 설명(호출 I)을 듣는다 — 음성은 저장하지 않고 글자 스크립트만(SPEC §21, `docs/harness/english.md` §12). 앱 전체 명세는 `docs/SPEC.md`(확장은 §14), AI 호출 상세는 `docs/harness/english.md`(과목 공통 규약은 `docs/HARNESS.md`), 디자인 원본은 `design/영어책_학습카드_샘플.html`.

저장소는 북카드 말고도 과목·영역을 더 기른다. 은우 쪽 **수학코치**(문제집 사진·직접 입력 → 3막 설명 + 검산 + 되감기 플레이어 — `docs/harness/math.md`), 그리고 학습자가 아이가 아니라 아빠인 **아빠 영역** 셋 — **아빠의 일본어**(JLPT 단어장·듀오링고 대화 복습·한자, 해설을 한국어까지 이어 읽는 낭독 — `docs/harness/japanese.md`, SPEC §18), **아빠의 영어**(토익스피킹, 코드·경로 `toeic`. 표현 암기장 사진 판독·파일 가져오기 → 발화 포인트 카드·전체 듣기·시험 4모드, AI가 새로 만든 11문항 모의고사 → 응시 녹음(기기에만)·버튼으로 도는 AI 채점·추정 등급 — `docs/harness/toeic.md`, SPEC §20. **교재 사진·전사본은 공개 저장소에 넣지 않는다** — `data/private/`(gitignore)에 두고 앱의 "파일로 가져오기"로만 넣는다)와 **아빠의 운동**(러시안 파이터 풀업·푸시업 사다리 추적. 계산은 **LLM 호출이 없는** 순수 엔진이라 하네스가 아니라 SPEC §19가 명세다 — 세션 음성 안내만 발음 엔진(클라우드 TTS)을 거친다. 총 운동 소요시간은 세션이 잰 실측, 세션 없이 한 기록·지난 기록은 엔진이 읽을 때 계산하는 근사치 — SPEC §19-8). 화면 공통으로 **언어별 발음 엔진**(기본은 영어·한국어 OpenAI TTS, 일본어 기기 음성 — 클라우드가 실패하면 기기 음성, SPEC §16·§16-5)과 모든 화면 상단의 **학습 스트릭**(은우 · 아빠 일본어 · 아빠 영어 · 아빠 운동을 나란히 — 은우 트랙은 단어장 시험에 자유대화를 더해 센다, SPEC §17)이 있다.

## ⚠️ 로컬 실행이 프로덕션 DB를 향할 수 있다 (2026-08-17 사고)

**이 저장소에서 앱이나 스크립트를 돌리기 전에 스토어 백엔드가 무엇인지 확인하라.**

`lib/store.ts`는 `STORE_BACKEND`가 없으면 자동 감지한다 — `GOOGLE_APPLICATION_CREDENTIALS`·`K_SERVICE`·`GOOGLE_CLOUD_PROJECT` 중 **하나만 있어도 Firestore**(=가족이 실제로 쓰는 프로덕션 DB)를 잡는다. 2026-08-17에 `.env`의 `STORE_BACKEND=firestore` 때문에 로컬 삭제 테스트가 실데이터를 지웠다 — 책 4권·카드 8장·읽음 기록 6건. PITR이 꺼져 있어 1시간 버전 보존 창이 닫히기 직전에 겨우 복구했다.

지금은 세 겹으로 막혀 있다:
- `.env`에서 두 줄을 주석 처리 → 로컬 자동 감지 결과가 `file`
- `lib/prod-guard.ts` → 프로덕션이 아닌데 Firestore를 지우려 하면 **던진다**(`ALLOW_PROD_DESTRUCTIVE=1`로만 해제)
- `getStore()` → 개발 환경에서 Firestore를 잡으면 경고를 찍는다

**그래도 규칙은 이것이다:**
- 앱·스크립트를 로컬에서 돌릴 때는 **`STORE_BACKEND=file`을 명시**하라. 자동 감지에 기대지 마라.
- 가드는 **삭제만** 막는다. 생성·수정은 그대로 통하므로, Firestore에 붙은 채 카드를 만들면 실데이터가 늘어난다.
- 서브 에이전트에게 검증을 시킬 때 이 사실을 프롬프트에 함께 넘겨라. 2026-08-17 사고의 직접 원인은 "무비용 검증만"은 지시하면서 **어느 DB를 향하는지는 확인하지 않은 것**이었다.

## 하네스: 은우학습 개발팀 (영어 + 수학 + 일본어 + 토익스피킹)

**목표:** 과목별 스펙(`docs/harness/english.md` · `docs/harness/math.md` · `docs/harness/japanese.md` · `docs/harness/toeic.md`)대로 앱을 구현하고, 품질(프롬프트)을 eval 기반으로 안전하게 튜닝한다.

**트리거:** 앱 구현·수정, AI 모듈(lib/ai), 프롬프트 튜닝, eval 실행, QA·정합성 검증 등 이 프로젝트의 개발 작업 요청 시 `study-orchestrator` 스킬을 사용하라. 영어(북카드)든 수학(수학코치)든 일본어(아빠의 일본어)든 토익스피킹(아빠의 영어 — `toeic`)이든 운동(아빠의 운동 — AI 없음, SPEC §19)이든 진입점은 하나다. 스펙 내용에 대한 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-16 | 초기 구성 (에이전트 4 + 스킬 4) | 전체 | - |
| 2026-08-17 | 서문에 "로컬 실행이 프로덕션 DB를 향할 수 있다" 경고 추가 | CLAUDE.md | 로컬 삭제 테스트가 프로덕션 Firestore를 지운 사고 — 에이전트에게 검증을 시킬 때 DB 대상 확인이 누락됐다 |
| 2026-08-17 | **과목 확장(영어 → 영어+수학)**: 에이전트 6명(재사용 4 + 신규 `math-verifier`·`player-builder`), 스킬 6종. `card-tuner`→`prompt-tuner`, `bookcard-qa`→`study-qa`, `card-eval`→`prompt-eval`, `bookcard-orchestrator`→`study-orchestrator`로 과목 중립화. 도메인 지식은 스킬 references로 분리 | agents 6, skills 6, CLAUDE.md | 수학코치 하네스 명세 반영. 에이전트를 과목별로 복제하면 역할이 중복되고 조율 비용만 커지므로, 에이전트는 재사용하고 references만 가르는 구조를 택했다 |
| 2026-08-16 | 검증 피드백 반영: worker 스킬 3종 description을 에이전트 스코프로 재작성(트리거 경합 해소), 오케스트레이터에 스캐폴딩 단계·튜닝 가드·에러 폴백 분기·eval 실행 주체·픽스처 출처 명시 | skills 4종 | Phase 6 검증(구조·충실도·트리거·드라이런) 결과 10건 수정 |
| 2026-08-16 | 개발 명세 원본 확보(docs/SPEC.md 저장): 픽스처를 §12 실측값으로 갱신(Pooh AR 2.0 — AR<2 경로 미커버 명시), Open Library 폴백·디자인 원본 파일 반영 | docs/SPEC.md, card-eval, ai-harness-impl, orchestrator | 사용자가 개발 프롬프트 전문 제공 |
| 2026-08-25 | `doc-commit` 스킬 신설 — 메모리·git 이력·코드 실체를 근거로 `.md` 문서를 최신화·커밋. 저장소 함정(spec-sync 프롬프트 블록 불가침·§번호 안정성·변경이력 append·next.js 자동블록)을 규칙으로 못박음 | skills(doc-commit), CLAUDE.md | 앱 기능이 문서보다 앞서 나가 서문·플로우가 실제와 어긋나기 시작 — 문서 동기화를 반복 가능한 절차로 |
| 2026-08-25 | 2026-08-16 이후 앱 확장을 문서에 반영: 서문 앱 흐름에 낭독 자막 grounding·챕터 리더·단어탭·영어단어장 정복 추가, SPEC에 §14(확장 기능)·환경변수 2종(`SUPADATA_API_KEY`·`YOUTUBE_API_KEY`) append, README 소개·환경변수 갱신. 반영 마지막 커밋 `4cabb21` | CLAUDE.md, docs/SPEC.md, README.md | 자막 grounding·단어장 정복 아크가 harness/english.md에만 있고 제품 문서엔 없었다 |
| 2026-09-02 | 2026-08-25 이후 앱 확장 3종을 문서에 반영: **목록 순서변경**(서재·단어장·수학, 관리모드 드래그+↑↓, 공유 프리미티브 `use-reorder`)·**읽어주기 속도 조절**(전역·localStorage 영속)·**단어장 유의어·반의어 연결 + 관계 문제 시험**을 SPEC §15에 추가, README §5 판단 기록 갱신. 반영 마지막 커밋 `fd3863d` | CLAUDE.md, docs/SPEC.md, README.md | 세 기능이 코드·git에만 있고 제품 문서엔 없었다 — doc-commit 스킬로 동기화 |
| 2026-09-02 | 유의어·반의어 연결을 **AI 추천 방식으로 재작업** 반영(a7df88e): SPEC §15-3의 진입 문장·상호기록 라우트·연결 UI를 갱신 — 기존 "단어장 내 항목만 나열" 피커가 아니라 호출 H(추천)로 실제 후보 제시 + 신규 추가(자동 보강)·연결. harness §11은 코드와 함께 이미 반영돼 불가침. 반영 마지막 커밋 `a7df88e` | docs/SPEC.md | 연결 대상이 단어장에 없으면 못 고르던 문제를 고쳐 UI·흐름이 §15-3 서술과 어긋났다 |
| 2026-09-18 | **과목 확장(영어+수학 → +일본어)**: `docs/harness/japanese.md` 신설(호출 A 단어 생성·B 대화 전사·C 학습 해설, 후리가나 토큰 규약, 시험 4모드, 저장 모델, J0~J5 로드맵). HARNESS 과목표·파일배치, CLAUDE.md 하네스 섹션, study-orchestrator 과목 판별 단서 갱신 | docs/harness/japanese.md, docs/HARNESS.md, CLAUDE.md, study-orchestrator | "아빠의 일본어"(JLPT 단어장 + 듀오링고 대화 복습) 요청. 학습자가 아이가 아니라 성인이라 눈높이 규칙이 반대고, 표기/읽기 2축·후리가나 때문에 영어 단어장 스키마를 그대로 못 쓴다 |
| 2026-09-24 | **아빠 영역 확장**: ① 대화 복습 해설 낭독(SPEC §18 — 일본어 문장 🔊 + 한국어 해설까지 이어 읽는 `speakQueue`, 클라우드 TTS에 `ko-KR` 추가, japanese.md §13 포인터) ② **아빠의 운동**(SPEC §19 — 러시안 파이터 풀업·푸시업 사다리, AI 없는 순수 엔진 `lib/workout.ts` + 사건 재생 상태 기계, `workoutCycles` 컬렉션, `/workout`). eval 2종(`eval:speech`·`eval:workout`, 오프라인) | docs/SPEC.md, docs/harness/japanese.md, CLAUDE.md | 사용자 요청 — 해설을 소리로 복습 + 운동 루틴 자동 추적. 운동은 결정적 규칙이라 LLM 코치 대신 순수 함수로(같은 입력=같은 답, 비용 0) |
| 2026-09-24 | 문서 동기화(doc-commit): 2026-09-02 이후 확장 — **아빠의 일본어**(J0~J5·한자)·**클라우드 발음**(§16)·**학습 스트릭**(§17, 운동 트랙 §17-7)·**해설 낭독**(§18)·**아빠의 운동**(§19)을 서문·README(소개·환경변수·판단 기록·하네스)·HARNESS(과목표·파일배치의 '(예정)'·'(구현 중)' 해소, 하네스 밖 두 종류 — LLM 없는 운동·스트릭 / Structured Outputs 밖인 클라우드 TTS — 구분, 일본어 eval은 사실상 오프라인)·SPEC §11(TTS·검산 모델 env + 빈 값 폴백 — 코드도 `??`→`||` 수정)·SPEC §16-5(언어별 엔진·기기 음성 품질·IndexedDB 영속 캐시 — 코드엔 있고 스펙엔 없던 것)·japanese.md(§6-1 단어 시험 5모드 — def-to-word, §8 홈 2×2, §9 실호출 게이트는 자리만)·study-orchestrator(과목 판별에 운동 단서 — '아빠'가 이제 일본어·운동 양쪽)에 반영. 반영 마지막 커밋 `667b377`(환경변수 빈 값 폴백 수정 포함) | CLAUDE.md, README.md, docs/HARNESS.md, docs/SPEC.md, docs/harness/japanese.md, study-orchestrator | 기능이 코드·SPEC에만 있고 서문·README·HARNESS·오케스트레이터는 영어+수학 시절에 머물러 있었다(구현된 수학·일본어 모듈을 아직 '예정'으로 표기) |
| 2026-09-24 | **하네스 보정 — 영어·일본어·과목 공통까지**(수학에 치우쳐 있던 하네스): 에이전트는 새로 만들지 않고(2026-08-17 원칙) references를 과목별로 나눴다 — `prompt-eval/references/japanese-dials.md`·`study-qa/references/japanese.md`·`study-qa/references/common.md`·`ai-harness-impl/references/{english-routes,japanese,app-patterns}.md` 신설, 영어 references 2종을 카드(호출 A·A′·B)에서 호출 A~H 전체·eval 게이트 6종·grounding 가드로 전면 갱신, ai-harness-impl 과목표 3과목화, 에이전트 4명(ai-engineer·app-builder·qa-inspector·prompt-tuner)과 prompt-eval·study-qa SKILL을 3과목+common으로, 오케스트레이터에 일본어·운동·공통 워크플로우·버그 분류(먼저 QA로 원인 → 사용자 확인 → 원인별 담당)·소리 신고 분기·과목별 eval 비용·해설 단서 분리, doc-commit에 japanese.md(JSON Schema 의미 동치 대조) 반영. 검증: 갈래별 적대 검증자가 코드와 대조(갈래마다 5~9건 정정), Phase 6 구조·트리거(near-miss 17문항)·드라이런 3종 — 끊긴 링크 0 | .claude/agents 4, .claude/skills 5(+references 6 신설), CLAUDE.md | 사용자 요청("하네스가 수학에 국한 — 언어와 일본어를 포함하게"). 영어 스펙은 11절로 컸는데 QA·튜닝 레퍼런스는 카드 생성 3호출에 머물렀고, 일본어 과목과 공통 기능(발음·스트릭·운동)은 검증·튜닝 가이드가 없었다 |
| 2026-09-26 | **과목 확장(+토익스피킹 = 아빠의 영어)** 문서 반영: 스펙 `docs/harness/toeic.md`(호출 A 표현집 판독·B 발화 포인트·C 모의고사 C1~C5 파트별 병렬·D 답변 피드백 + 하네스 밖 관문 **P** 사진 생성(`lib/toeic-image.ts`)·**T** 음성 전사(`lib/toeic-transcribe.ts`)), 컬렉션 5개(`toeicSets`·`toeicQuizzes`·`toeicMocks`·`toeicImages`·`toeicAttempts`), 스트릭 아빠 **🎙️ 영어 트랙**(표현 시험 답한 문항≥1·응시 녹음된 문항≥1, 폰 "일" 생략·세 자리부터 사람 이모지 숨김 — SPEC §17-8), 제품 흐름·경계·비용·실기기 확인 SPEC §20, env 3종(`OPENAI_IMAGE_MODEL`·`OPENAI_IMAGE_QUALITY`·`OPENAI_TRANSCRIBE_MODEL`, 빈 값 폴백 — §11·.env.example), HARNESS(AI 과목 넷·과목표·하네스 밖 관문 P/T·파일 배치·`eval:toeic` 기본 오프라인 + `EVAL_TOEIC=1` 게이트), README(소개·환경변수·eval·판단 기록), 서문·하네스 섹션 과목 목록. **교재 내용 저장소 밖 원칙**을 SPEC §1·§13·§20-6에 명시. 코드는 아직 커밋 전(작업 트리 — 마지막 커밋 `f765e3e` 위). 하네스 에이전트·스킬(study-orchestrator·study-qa·ai-harness-impl·prompt-eval)에는 아직 토익 줄이 없다(harness 보정 몫) | CLAUDE.md, README.md, docs/SPEC.md, docs/HARNESS.md, .env.example | 사용자 요청 — 토익스피킹 모의시험 + 표현집 사진 → 은우 단어장과 같은 낭독·시험 + 교재 10쪽 미리 넣기. 학습자가 성인이고 목표가 뜻 알기가 아니라 "말로 꺼내 쓰기"라 은우 단어장 스키마·프롬프트를 못 쓰고, 은우 컬렉션과 섞으면 은우 스트릭·오답노트가 오염된다. 저장소가 PUBLIC이라 교재 사진·전사를 커밋하면 출판물이 공개되므로 교재 반입을 앱의 파일 가져오기로만 열었다 |
| 2026-09-26 | **하네스 편입 — 토익(아빠의 영어)**: 에이전트는 복제하지 않고(2026-08-17 원칙) references만 늘렸다 — `ai-harness-impl/references/toeic.md`(진입 함수가 client.ts가 아니라 `lib/ai/toeic/calls.ts`, 하네스 밖 관문 P/T, 60초 상한 분할, 부분 성공·빈 자리만·먼저 준비된 사진, spec-sync 14 + JSON Schema 8 + 호출 옵션 문장, 새 export)·`study-qa/references/toeic.md`(환각 차단 zod·said 순서·모드 분리·같은 뜻 보기 제외 반례, 재완료 409·동시 채점 트랜잭션, 오디오 세션 순서·무음 질문 안전망·gUM 상한, 비용 가드, 교재 원문 오염 스캔, e2e 가짜 마이크·시간 배율·스텁 4종, iPhone 목록)·`prompt-eval/references/toeic-dials.md`(B·D는 여유 폭 없음, C 폭은 등급 무관, 정확성 장치·저작권) 신설. subject `toeic`을 오케스트레이터(과목 판별 — "표현집"≠단어장, "영어"·"아빠"만으로는 못 가름, 워크플로우 A·B, 비용표 `eval:toeic` 게이트 3~4회·화면 경로의 자동 포인트·자동 사진, 소리 신고 3경로, 안전 — PUBLIC·`data/private` 읽기 전용·루프백 스텁)·에이전트 4명·SKILL 3종 과목표·doc-commit spec-sync 목록(toeic.md)에 반영. `app-patterns.md` §15(녹음·기기 전용 보관·파일로 가져오기 멱등·60초 상한·하네스 밖 관문 모양) 신설, `DestructiveOp` 9종·reorder 라우트 7·페이지/라우트 수 갱신. 같은 날 P2 수정(`speakQueue` `onEnd` 둘째 인자 `sounded`, gUM 상한)을 `app-patterns` §8·`common.md` §0·§2에 반영(eval:speech 113, eval:streak 47, eval:toeic 357) | .claude/agents 4, .claude/skills 5(+references 3 신설), CLAUDE.md | 바로 윗줄의 토익 문서 반영이 "하네스에는 아직 토익 줄이 없다"로 남겼다 — CLAUDE.md 트리거는 토익을 study-orchestrator로 보내는데 과목 판별 단서·references·비용표가 없어 에이전트가 영어(은우) 스펙으로 작업할 위험이 있었다 |
| 2026-09-26 | **운동 총 소요시간 + 은우 자유대화** 문서 반영(doc-commit — 마지막 커밋 `16524fb` 이후 작업 트리의 코드 기준): ① **아빠의 운동 총 소요시간**(SPEC §19-8 — 세션이 잰 실측 `durationSec`(클라이언트 보고값, 정수 0~3시간 밖은 기록은 받고 null)·세션 없이 한 기록과 지난 기록은 읽을 때 엔진이 계산하는 근사치(저장·백필 없음)·오늘 기록 카드·누적·지난 사이클·📒 운동 기록·세션 경과 시계. §19-2 사건 타입·§19-4 스토어 계약·§19-5 zod `.default(null)`·§19-6 진행 보존 `startedAt`과 0스텝 저장·§19-7 eval import 예외) ② **은우 자유대화**(SPEC §21 — **관문 R**(OpenAI Realtime WebRTC, 하네스 밖·키 규약만 공유)·**호출 I**(문장 설명, `callWithSchema`)·**화면 카드**(말문 막힘 도움 5초/12초·이모지 그림 카드·주제 일러스트 — 사진 공용 코어 `lib/image-gen.ts`)·**스트릭**(은우 트랙에 발화 ≥1 대화, §17-9)과 구현이 정한 동작 — 저장 멱등 `clientSessionId`=문서 id·뒤로가기/숨김/pagehide 종료·keepalive 바이트 판정·응답 진행 중 요청 보류·도구만 연쇄 상한 2·이모지 판정·✓ 매칭, §21-8 알려진 한계 신설). english.md §1·§5(게이트 `EVAL_TALK`)·§12-0(구현 파일 표)·§12-1~§12-6 산문(코드블록·spec-sync 불가침), HARNESS §0·§3·§4(관문 R·사진 공용 코어), README 소개·환경변수 3종·판단 기록, `.env.example` | docs/SPEC.md, docs/harness/english.md, docs/HARNESS.md, CLAUDE.md, README.md, .env.example | 두 기능이 코드·QA 리포트에는 있고 문서는 구현 전 설계 문장에 머물러 있었다 — 구현이 채운 스펙 공백(멱등 키·응답 진행 중 보류·연쇄 상한·그림 문자 판정)과 QA 수정(뒤로가기 종료·keepalive 바이트)이 문서에 없으면 다음 작업이 옛 문장대로 되돌린다 |
| 2026-09-26 | **하네스 편입 — 은우 자유대화 + 운동 소요시간**: 에이전트는 새로 만들지 않고(2026-08-17 원칙) 영어·공통 references만 늘렸다 — `ai-harness-impl/references/english-routes.md` §7(관문 R: 서버가 SDP를 대신 넘기고 미디어는 직통이라 60초 상한 무관·키 노출 0, 세션 설정·입력 정리, 라우트 9개, 앱 안내 = 숨은 system 메시지 + 인자 없는 `response.create`(응답 단위 instructions 금지), 도구 호출 처리·연쇄 상한 2, 저장 멱등·자르기, 호출 I 과금 전 순서, 개발 전용 가짜 전송, 알려진 틈)·`app-patterns.md` §16(전이중 `acquireMicStream`·사진 공용 코어 `lib/image-gen.ts`·멱등 키 = 문서 id·keepalive **바이트** 판정과 거부 시 1회 일반 요청·언마운트 정리) + §14 운동 소요시간(정규화 함정·zod는 형식만/상한은 엔진·근사는 저장 안 함), `study-qa/references/english.md` §8(정합성 매트릭스·리듀서 반례·도움 상태 기계 독립 참조 모델·키/프롬프트 노출·가짜 전송 무력화·뒤로가기·저장 멱등·iPhone 12항목)·`common.md` §4-1(소요시간 독립 참조 모델 대조)·§3(은우 트랙 자유대화), `prompt-eval/references/english-dials.md` §10(선생님 차례 길이·질문 수·한국어 사용량·도움 카드 답 예시 수·설명 대본 조각 수 + 튜닝 금지: 안전 규칙·전사 prompt·keyWords ⊂ 문장·1학년 눈높이), study-orchestrator(과목 판별 — 자유대화 vs 일본어 '대화 복습'·토익 '녹음', 소리 신고 3경로, 비용: 📞 = Realtime 분 단위 + 일러스트 1장·문장 설명 호출 I·`EVAL_TALK=1` 2회), 에이전트 4명·SKILL 4종·토익 references 2종(사진 코어·마이크 공유 한 줄)·doc-commit 함정(english.md §12 `block-exact`·JSON 의미 동치·산문 needle). 오프라인 eval english 454·toeic 357·japanese 112·math 51·streak 53·workout 158 전부 PASS(문서만 변경) | .claude/agents 4, .claude/skills 5(SKILL) + references 7, CLAUDE.md | 자유대화(관문 R·호출 I·화면 카드)와 운동 소요시간이 코드·SPEC에만 있고 하네스는 북카드·단어장·토익 시절에 머물러 있었다 — "화면이 사라져도 과금이 이어진다"(QA P1)·"keepalive를 글자 수로 잰다"(P2-A)·"선생님 음성은 eval 밖"을 다음 작업자가 모르면 같은 사고를 되풀이한다 |
| 2026-09-26 | **자유대화 확정 결정 동기화**(doc-commit — QA `qa_report_english_talk_4.md` P2-D, 코드 변경 없음): "사용자 결정 대기·사용자 판단·열린 결정·미정·QA 재검증 대기"로 남아 있던 곳을 확정 사실로 — ① 도움 요청 은우 발화 전 **연속 2번**(12초 자동·🙋 합산, 상한이면 카드만·선생님 말하는 중 🙋는 보류 없음) ② 이모지 칸 그림 문자 필수(도형 기호 블록 허용·라틴/한글 금지) ③ 호출 I `[말투]` 예시 교체 완료 ④ 끝내기 버튼·5분 마무리 뒤 종료는 전사 중 은우 말을 최대 2.5초 대기(숨김·뒤로가기·pagehide는 즉시) — `docs/harness/english.md` §12-1에 산문 추가(턴 감지 끄기·수동 커밋·취소·소리 비우기) ⑤ 연결 전 끝내기 문구 ⑥ 운동 실측 시작점 = 세션을 처음 연 순간. SPEC §21-8 제목에서 "사용자 결정 대기"를 뺐다(§번호 그대로). QA 3·4 재검증 통과 반영(keepalive 바이트·도형 이모지·카드 접힘 잠금·저장 본문 eval 12행·상한 eval 잠금 P2-C), `eval:english` 항목 수 454 → **484**(자유대화 308 — 도움 47·저장 본문 12 신설), study-qa §8(상한 참조 모델·끝내기 기다림 e2e 항목·실기기 13·실연결 목록), 토익 references의 "QA 재검증 전" 2곳도 통과로 | README.md, docs/SPEC.md, docs/harness/english.md, .claude/skills(ai-harness-impl references 3·prompt-eval english-dials·study-qa english·study-orchestrator), CLAUDE.md | 결정은 코드·SPEC에 들어갔는데 README 두 줄은 사실과 반대였고, 하네스 references가 "사용자 결정 대기"로 남아 다음 작업자가 이미 끝난 결정을 다시 묻거나 상한 없는 옛 동작을 기준으로 검증할 수 있었다 |

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
