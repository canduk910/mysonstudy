---
name: study-qa
description: "qa-inspector 에이전트가 검증 작업을 수행할 때 로드하는 스킬(과목 공통). 구현이 과목 스펙과 일치하는지, 프롬프트↔JSON Schema↔zod↔eval 4중 정의가 어긋나지 않는지, API↔프론트 경계면이 맞물리는지 교차 검증하는 방법론·체크리스트·리포트 형식을 담는다. 과목별 정합성 매트릭스는 references/english.md·math.md에 있다. 사용자의 검증·QA 요청 진입점은 study-orchestrator 스킬이다."
---

# Study QA — 통합 정합성 검증 (과목 공통)

**"존재 확인"이 아니라 "경계면 교차 비교"가 이 일의 본질이다.** 각 파일을 따로 보면 전부 올바른데 연결 지점에서 어긋나는 결함이 이 프로젝트에서 반복적으로 나왔다.

## 어느 references를 읽을 것인가

| 과목 | references | 핵심 검증 축 |
|---|---|---|
| 영어 (북카드) | `references/english.md` | 4중 정의 매트릭스, 카드 구조 개수·비율 |
| 수학 (수학코치) | `references/math.md` | 답 정확성(심판 2겹), 장면 검산, iframe 격리 |

## 왜 4중 정의가 위험한가

같은 값이 **프롬프트 ↔ JSON Schema ↔ zod ↔ eval** 네 곳에 각각 산다. 한 곳만 고치면 나머지가 조용히 어긋나고, 그 어긋남은 대개 실호출을 해야 드러난다. 그래서 값 하나를 네 곳에서 동시에 대조하는 매트릭스를 만든다.

## 실측을 이겨라 — 이 프로젝트에서 실제로 나온 결함들

주장이 아니라 **실행**으로 확인하라. 아래는 정적 검사로는 안 잡혔던 것들이다.

| 결함 | 왜 정적으로 안 잡혔나 |
|---|---|
| 두 함수가 같은 입력에 다른 판단 → 호출이 확정 실패 | 각각은 타입이 맞았다. 두 함수를 같은 입력으로 **함께 실행**해야 드러난다 |
| 신규 데이터에서 배지가 사라짐(`===` 비교가 항상 false) | `tsc` 통과. 구·신 데이터를 **실제로 렌더**해야 보인다 |
| 상한이 API·UI 두 곳에 흩어져 UI가 먼저 잘라냄 | 두 숫자가 각각 유효했다. **관통 경로**를 따라가야 보인다 |
| `capture` 속성이 `multiple`을 무력화 | 브라우저·OS 동작이라 코드에 흔적이 없다. **실기기**에서만 드러난다 |
| 배치 병렬 호출 간 표기 불일치 | 스텁은 늘 같은 응답을 준다. **실물 산출물**을 사람이 읽어야 보인다 |

여기서 나오는 원칙: **"규칙이 존재한다"가 아니라 "틀린 입력을 실제로 거부한다"를 확인하라.** 특히 수학의 검산 규칙은 오답을 실제로 잡는지 반례를 넣어 봐야 한다.

## 경계면 체크리스트

- API 응답 shape ↔ 프론트가 읽는 필드 — 이름·타입·nullable을 **동시에 열어 비교**
- 에러 code ↔ 상태코드 ↔ 프론트 분기 — 중간에서 끊기면 사용자는 무슨 일인지 모른다
- 상수의 정의처가 하나인가 — 같은 숫자가 두 곳에 있으면 그 자체가 결함
- 폴백 경로가 실제로 실행되는가 — 실패를 인위적으로 만들어 확인
- 하위 호환 — 기존 저장 데이터가 새 코드에서 정상 렌더되는가
- 키 노출 — `.next/static` grep으로 실제 키·프롬프트 원문 0건 확인

## 비용·안전 (이 프로젝트 특유)

- **OpenAI 실호출 금지.** 무비용 검증(`tsc`·`build`·`seed`·루프백 스텁·dev 서버 렌더)만 한다. eval은 오케스트레이터가 사용자 동의를 받아 실행한다.
- **로컬 실행은 `STORE_BACKEND=file`을 명시한다.** 이 저장소는 로컬 실행이 프로덕션 Firestore를 향할 수 있었고, 실제로 그 경로로 데이터가 지워진 적이 있다(CLAUDE.md 서문). 삭제는 `lib/prod-guard.ts`가 막지만 **생성·수정은 여전히 통한다.**
- 검증에 쓴 임시 데이터는 **원상 복구하고 잔존 0건을 확인**한다. 접두사 기반 정리는 UUID를 쓴 잔존물을 놓친다 — 개수로도 대조하라.
- **코드를 고치지 마라.** 검증·보고만 한다. 수정은 담당 에이전트가 한다.

## 리포트 형식

`_workspace/qa_report_{subject}_{n}.md`

```
# QA 리포트 {n} — {검증 범위}
## 요약: 통과 X / 실패 Y / 미검증 Z
## 정합성 매트릭스 (과목별 references 기준)
## 항목별 pass/fail 표 — 각 실패에 담당(에이전트)과 P1/P2
## 실측 방법 — 무엇을 어떻게 실행해 확인했나
## 미검증 항목과 그 이유
## 배포 가부 (가능/보류) + 근거 한 줄
```

**실패마다 담당과 우선순위를 명시한다.** 오케스트레이터가 그것으로 수정 루프를 돌린다. 배포 가부는 마지막에 한 줄로 못 박는다 — "P1이 하나라도 있으면 보류"가 기본이다.

**검증하지 않은 것을 검증했다고 쓰지 마라.** 미검증은 미검증으로 남기는 편이 훨씬 낫다.
