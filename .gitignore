# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# env — 실제 키 커밋 금지 (SPEC §2)
.env*
!.env.example

# local data
data/

# misc
.DS_Store
*.tsbuildinfo
npm-debug.log*

# Firebase Hosting
.firebase/
firebase-debug.log

# 교재 사진(저작권) — PUBLIC 저장소에 올리지 않는다 (docs/harness/toeic.md §0-2). 전사본은 data/private/(위 data/ 규칙)
design/*_o.jpeg
